# AGENT.md

This file exists because the user explicitly asked for `AGENT.md`.
The canonical agent instruction file for this repository is `AGENTS.md`.

Before doing Pake build/install work, read:

- `AGENTS.md`
- The `Personal Pake Automation` section in `AGENTS.md`
- `scripts/pake-ops.mjs --help`

Prefer `node scripts/pake-ops.mjs ...` for repeated Pake app build,
artifact download, DMG validation, installation to `/Applications`, and
lightweight verification. This keeps future turns shorter and reduces repeated
approval prompts.
